-- Loading screen menu items
-- Displayed during GameState.LOADING while world generates and player spawns

return function()
    return {
        -- No interactive items during loading - just displays "Loading..."
    }
end
